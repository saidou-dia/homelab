---
# Installer les paquets système essentiels sur les PVE
- name: Installer les paquets essentiels
  apt:
    name:
      - vim
      - git
      - htop
      - curl
      - wget
    state: present
    update_cache: yes
  when: "'pve' in group_names"


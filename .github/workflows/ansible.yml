name: CI Ansible

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  lint-and-syntax-check:
    name: Lint & Syntax Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install Ansible and tools
        run: |
          pip install ansible ansible-lint

      - name: Run ansible-lint
        run: |
          ansible-lint ansible/

      - name: Check syntax (site.yml)
        run: |
          ansible-playbook -i ansible/inventories/dev/hosts.yml ansible/site.yml --syntax-check

  dry-run:
    name: Dry Run (Check Mode)
    runs-on: ubuntu-latest
    needs: lint-and-syntax-check

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install Ansible
        run: |
          pip install ansible

      - name: Inject vault password from GitHub Secrets
        run: |
          echo "${{ secrets.ANSIBLE_VAULT_PASSWORD }}" > ansible/vault_pass.txt

      - name: Run Ansible playbook in check mode (no changes)
        run: |
          ansible-playbook \
            -i ansible/inventories/dev/hosts.yml \
            ansible/site.yml \
            --vault-password-file ansible/vault_pass.txt \
            --check


# Optionnel: futur job pour appliquer en prod
#  apply-prod:
#    name: Apply on Production
#    runs-on: ubuntu-latest
#    needs: dry-run
#    steps:
#      - name: Checkout repository
#        uses: actions/checkout@v4
#
#      - name: Set up Python
#        uses: actions/setup-python@v5
#        with:
#          python-version: "3.10"
#
#      - name: Install Ansible
#        run: pip install ansible
#
#      - name: Inject vault password
#        run: echo "${{ secrets.ANSIBLE_VAULT_PASSWORD }}" > vault_pass.txt
#
#      - name: Apply playbook on production nodes
#        run: |
#          ansible-playbook \
#            -i inventories/prod/hosts.yml \
#            site.yml \
#            --vault-password-file vault_pass.txt
